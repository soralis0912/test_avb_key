# IPA 脆弱性関連情報 届出書（案）

**件名：Android 端末が AOSP 公開テスト鍵（既知の秘密鍵）で署名された vbmeta を搭載しており、Android Verified Boot を迂回して恒久的な改ざんが可能**

> IPA「ソフトウエア製品の脆弱性関連情報に関する届出」フォームへの転記用。
> 本届出は**一次報告**であり、詳細なログ・バージョン情報等は追加提出可能です（第 5 節参照）。

---

## 0. 既届出との関係（冒頭に明記）

本届出は、当方が過去に届け出た以下の案件と **同一の脆弱性（同一原因・同一の攻撃手法）** であり、
**影響を受ける製品が新たに判明したため、対象製品を追加して届け出るもの**です。

| 受付番号 | 備考 |
| --- | --- |
| IPA#11335265 | 同一の脆弱性（既届出） |
| IPA#33436631 | 同一の脆弱性（既届出） |
| IPA#47427200 | 同一の脆弱性（既届出） |

根本原因はいずれも「製品出荷（user ビルド）の vbmeta が、AOSP のソースツリーに秘密鍵ごと
公開されているテスト用署名鍵（`external/avb/test/data/testkey_rsa*.pem` 等）で署名されている」ことです。
上記既届出と重複する内容については、そちらの記載内容を援用いただいて差し支えありません。

本届出では上記既届出と異なり、**2 機種いずれについても実機で改ざんイメージの書き込みと
起動成功まで確認**しています（4-3 参照）。

---

## 1. 届出者情報

| 項目 | 内容 |
| --- | --- |
| 氏名 | **松原 正佳** |
| フリガナ | マツバラ マサヨシ |
| メールアドレス | m.mt.br.527@gmail.com |
| 電話番号 | 記載なし（IPA 届出フォームの任意項目のため。連絡はメールにてお願いします） |
| 届出者情報の製品開発者への通知 | **可**（氏名・連絡先を製品開発者へ通知いただいて差し支えありません） |
| 公表時の発見者名の掲載 | **希望する**（発見者名：松原 正佳） |

---

## 2. 発見情報

| 項目 | 内容 |
| --- | --- |
| 発見日 | **2026年7月21日** |
| 発見の経緯 | 市販の Android 端末の出荷ファームウェアについて、Android Verified Boot（AVB）の署名鍵が製品固有の鍵であるかを、公開されている検査ツール（NCC Group `test_avb_key`）を用いて自主的に調査していたところ、本事象を確認した。その後、当方が正当に保有する実機において、改ざんイメージが実際に Verified Boot を通過して起動することを確認した。 |
| 第三者による悪用の有無 | 未確認 |

---

## 3. 製品情報

いずれも **検証時点で入手可能な最新のファームウェア**において事象を確認しています
（＝アップデートを適用済みの端末でも解消されていません）。

### 対象製品 1

| 項目 | 内容 |
| --- | --- |
| 製品名 / 型番 | Rakuten Hand 5G（P780） |
| バージョン | 検証時点の最新ファームウェア |
| 製品開発者名 | 楽天モバイル株式会社（販売元）／ TINNO Mobile Technology Corp.（製造元） |
| 検証形態 | **実機検証済み**（改ざんイメージの書き込み・起動成功まで確認） |

### 対象製品 2

| 項目 | 内容 |
| --- | --- |
| 製品名 / 型番 | dtab Compact d-52C |
| バージョン | ビルド番号 **202502141452**（検証時点の最新ファームウェア） |
| 製品開発者名 | 株式会社NTTドコモ（販売元）／ Lenovo（製造元） |
| 検証形態 | **実機検証済み**（EDL モード＋firehose プログラマ経由の書き込み・起動成功まで確認） |

> 補足：本脆弱性は特定の 1 機種に固有のものではなく、同一のビルド構成を共用する
> 他の機種・他のロットにも波及している可能性があります。製品開発者においては、
> 出荷中および出荷済みの全機種について同様の確認を行うことを推奨します。

---

## 4. 脆弱性情報

### 4-1. 脆弱性の種類

- 既知の（公開された）暗号鍵の使用 — CWE-321: Use of Hard-coded Cryptographic Key
- 関連：CWE-798（ハードコードされた認証情報の使用）、CWE-347（暗号署名の不適切な検証）
- 分類（IPA 選択肢）：**その他（暗号技術の不適切な使用 / セキュアブートの迂回）**

### 4-2. 脆弱性の内容

Android 端末では、ブートローダが `vbmeta` パーティションの署名を検証し、その内容（各パーティションの
ハッシュ／ハッシュツリー記述子）に基づいて `boot` / `init_boot` / `system` / `vendor` などのパーティションの
完全性を検証する Android Verified Boot（AVB）が実装されています。この仕組みの安全性は、
**vbmeta の署名鍵の秘密鍵が製品開発者のみに秘匿されていること**を前提としています。

しかし対象製品の出荷ファームウェア（user ビルド）の `vbmeta` は、**AOSP のソースツリー内に
秘密鍵そのものが公開されているテスト用鍵**（`external/avb/test/data/testkey_rsa*.pem`。
本来は userdebug／開発ビルド専用）で署名されていました。

この秘密鍵は誰でも取得可能であるため、攻撃者は **任意に改変した boot / init_boot / system / vendor イメージと、
それに対応する vbmeta を自ら正当に署名して作成できます**。生成した vbmeta はブートローダの検証を
正規に通過するため、ブートローダがロックされたままの状態（Verified Boot state = GREEN、警告画面なし）
でも、改ざんされた OS が正規のものとして起動します。

すなわち、ブートローダのロックや e-Fuse による Root of Trust の設定が正しく行われていても、
**Verified Boot による改ざん検知が事実上無効化されている**状態です。

**本脆弱性が特に重大である理由**は、AVB が本来担っている「一時的な権限昇格や一時的な物理アクセスを、
恒久的な端末乗っ取りに昇格させない」という防御線が機能しない点にあります。通常、何らかの手段で
パーティションを書き換えられたとしても、次回起動時に Verified Boot が改ざんを検知して起動を拒否する
ため、侵害は再起動により封じ込められます。しかし本脆弱性が存在する端末では、攻撃者が公開テスト鍵で
正当な署名を付与できるため、**再起動後も検知されずに改ざんが維持され、初期化（factory reset）でも
除去されない恒久的な侵害が成立します**。当方はこれを 2 機種の実機で確認しています。

なお、AVB の公開鍵（またはそのハッシュ）はブートローダ側／e-Fuse 側に焼き込まれているため、
本問題の是正には通常、ブートローダを含めた署名鍵の入れ替えが必要となり、
**通常の OTA によるアプリ層の修正では解消できない**点にご留意ください。

参考：本問題の一般的な解説および検査ツール（NCC Group）
- https://www.nccgroup.com/research-blog/on-almost-signing-android-builds/
- https://github.com/nccgroup/test_avb_key
- Android Verified Boot: https://source.android.com/docs/security/features/verifiedboot

### 4-3. 再現手順（検証方法）

#### (1) テスト鍵で署名されていることの確認（両機種共通）

1. 対象端末の `vbmeta` イメージを取得する（公式配布ファームウェアからの抽出、または端末からの吸い出し）。
   ```
   dd if=/dev/block/by-name/vbmeta of=/sdcard/vbmeta.img
   ```
2. AOSP の公開テスト鍵を取得する。
   ```
   git clone https://android.googlesource.com/platform/external/avb
   # 秘密鍵は avb/test/data/testkey_rsa2048.pem, testkey_rsa4096.pem, testkey_rsa8192.pem 等
   ```
3. 公開検査ツールで、vbmeta 内に公開テスト鍵の公開鍵（モジュラス）が含まれるかを検査する。
   ```
   git clone https://github.com/nccgroup/test_avb_key
   python3 test_avb_key.py ./vbmeta.img ./avb/test/data/
   ```
4. テスト鍵で署名されている場合、該当鍵について以下の出力が得られる。
   両機種とも AOSP 公開テスト鍵との一致を確認済み。

   ```
   Using known private key for verification: ./avb/test/data/testkey_rsa4096.pem. Public key found at index: <offset>
   If the script was executed on a vbmeta.img file from an Android user build, there is a problem.
   ```

5. 補助的確認として、以下でも同一の公開鍵が使用されていることを確認できる。
   ```
   avbtool info_image --image vbmeta.img
   python3 avbtool extract_public_key --key testkey_rsa4096.pem --output pubkey.bin
   # vbmeta.img 内に pubkey.bin と一致するバイト列が存在すること
   ```

#### (2) 実機における影響の実証 ― Rakuten Hand 5G

**ブートローダのアンロックを行わずに**（＝出荷状態のロックされたブートローダのまま）
改ざんイメージが起動することを確認しました。

1. 端末上のローカル権限昇格の脆弱性（Qualcomm Adreno GPU の **KGSL ドライバの脆弱性**）を利用し、
   root 権限を取得した。※当該脆弱性の詳細は本届出の対象外であり、別途扱いとさせてください。
2. 取得した root 権限で `init_boot`（および `vbmeta`）パーティションの内容を改変した。
3. 改変後のイメージに対し、**公開されている AOSP テスト鍵（秘密鍵）を用いて `avbtool` で署名**し、
   対応する `vbmeta` を再生成した。
   ```
   avbtool add_hash_footer --image init_boot_modified.img --partition_name init_boot \
       --partition_size <SIZE> --key testkey_rsa4096.pem --algorithm SHA256_RSA4096
   avbtool make_vbmeta_image --key testkey_rsa4096.pem --algorithm SHA256_RSA4096 \
       --include_descriptors_from_image init_boot_modified.img ... --output vbmeta_modified.img
   ```
4. 生成したイメージを各パーティションへ書き込んだ。
   ```
   dd if=init_boot_modified.img of=/dev/block/by-name/init_boot
   dd if=vbmeta_modified.img   of=/dev/block/by-name/vbmeta
   ```
5. 再起動したところ、**警告画面や起動拒否は発生せず、改ざんしたイメージがそのまま起動した**。
   ブートローダはロックされたままであり、Verified Boot の検証は「正常」として通過した。

   → 署名鍵が製品固有の秘匿鍵であれば手順 3 の署名を作成できないため、手順 5 で起動が拒否され、
     一時的に取得した root 権限は再起動により無効化されるはずである。本脆弱性によりそれが成立していない。

#### (3) 実機における影響の実証 ― dtab Compact d-52C

本機種では、**端末側の脆弱性を利用した権限昇格を必要とせず、物理的なアクセスのみで**
改ざんイメージの書き込みが可能でした。

1. Lenovo が提供する公式ツール経由で、当該機種の**工場出荷用ファームウェア一式を入手**した。
   このパッケージには、EDL（Emergency Download）モードで動作する**署名済みの firehose
   プログラマ**が含まれており、一般の利用者・第三者が正規の手段で入手できる状態にある。
2. 端末を EDL モードで起動し、上記 firehose プログラマ経由で任意のパーティションへ
   読み書きできることを確認した（この段階でブートローダのアンロックは不要）。
3. `vbmeta` を吸い出し、(1) の手順で **AOSP 公開テスト鍵で署名されていること**を確認した。
4. 任意に改変したイメージと、それに対応する `vbmeta` を**公開テスト鍵で署名して生成**し、
   firehose 経由で書き込んだ。
5. 再起動したところ、**警告画面や起動拒否は発生せず、改ざんしたイメージがそのまま起動した**。

   → EDL 経由の書き込み自体は工場出荷・修理工程で必要な機能であり、本来は
     「書き込めても Verified Boot が起動時に検知して拒否する」ことで安全性が担保されます。
     署名鍵が公開テスト鍵であるため、この最後の防御線が機能していません。
     結果として、**端末を一時的に手にできる第三者は、権限昇格の脆弱性を用いることなく、
     利用者に気付かれない恒久的な改ざんを施すことが可能**です。

> **検証範囲について**：検証は当方が正当に保有する端末に対してのみ実施しており、
> 第三者の端末、通信事業者の設備、製品開発者のシステムに対する試験・改変は一切行っていません。
> 攻撃コードの公開も行っていません。

### 4-4. 脆弱性により発生しうる脅威

1. **セキュアブート（Verified Boot）の迂回**：改ざんされた OS が、警告表示なく正規イメージとして起動する。
   利用者は端末が改ざんされていることを認識できない（実機で確認済み）。
2. **侵害の恒久化**：一時的な権限昇格や一時的な物理アクセスを、**再起動やファクトリーリセットでも
   除去されない恒久的なバックドア**に転化できる。本来 AVB が担うべき「侵害の封じ込め」が機能しない。
3. **端末上での恒久的な任意コード実行**：root 権限で動作するコードの埋め込み、SELinux ポリシーの無効化、
   `init_boot` / `boot` / `system` / `vendor` パーティションへのバックドア設置。
4. **利用者情報の窃取**：認証情報、通信内容、メッセージ、位置情報、決済アプリの取扱い情報等の窃取。
   改ざんした OS によるロック画面の偽装等も理論上可能。
5. **端末セキュリティ機構の無効化**：Verified Boot に依存する各種保護（改ざん検知、
   Key Attestation を利用したアプリ側の完全性検証等）の信頼性が損なわれる。
6. **サプライチェーン／中古端末流通経路での悪用（いわゆる evil maid 攻撃）**：
   端末を一時的に扱える立場の者（修理・中古買取・輸送・貸与端末の返却時等）が、
   検知困難な改ざんを施した端末を利用者に渡すことが可能。

**攻撃条件**：パーティションへ書き込む経路を確保できること。当方が実機で確認した経路は次の 2 通りで、
**いずれもブートローダのアンロックを必要とせず**、利用者による操作や承認も不要です。

| 機種 | 確認した書き込み経路 | 必要な前提 |
| --- | --- | --- |
| Rakuten Hand 5G | ローカル権限昇格の脆弱性（KGSL ドライバ）で root 権限を取得し、パーティションへ書き込み | 端末上でのコード実行 |
| dtab Compact d-52C | EDL モード＋公式ツール経由で入手できる署名済み firehose プログラマ経由で書き込み | **端末への物理アクセスのみ**（脆弱性の利用は不要） |

**CVSS v3.1（参考値・届出者評価）**

- Rakuten Hand 5G（root 権限取得を経由する場合）
  - 基本値：**8.2（High）**／ベクタ：`CVSS:3.1/AV:L/AC:L/PR:H/UI:N/S:C/C:H/I:H/A:H`
  - 選定理由：root 権限の取得を前提とする（PR:H）が、その一時的な侵害をブートローダのロックという
    端末の信頼境界を越えて恒久化させるため Scope は Changed（S:C）とした。
- dtab Compact d-52C（EDL 経由・物理アクセスのみの場合）
  - 基本値：**7.6（High）**／ベクタ：`CVSS:3.1/AV:P/AC:L/PR:N/UI:N/S:C/C:H/I:H/A:H`
  - 選定理由：追加の脆弱性を必要とせず、事前の権限も利用者操作も不要（PR:N / UI:N）。

### 4-5. 回避策・対策

**利用者側の回避策**
- 有効な回避策はありません（署名鍵はファームウェアに固定されており、利用者設定では変更できません）。
  最新ファームウェアへのアップデートによっても解消されません（検証時点の最新版で確認）。
- 緩和策は、端末を第三者に物理的に預けない、信頼できない中古端末・貸与端末を業務利用しない、
  といった運用上の注意にとどまります。

**製品開発者側の推奨対策**
1. 出荷（user）ビルドの署名を、**製品開発者が秘匿管理する製品固有の AVB 鍵**に切り替える。
2. 併せて、ブートローダ／e-Fuse 側に焼き込まれた Root of Trust（AVB 公開鍵ハッシュ）を
   更新する必要があるため、出荷済み端末への配信可否・手順を検討する。
3. リリースビルドで `external/avb/test/data/` 配下の鍵が使用されないよう、
   ビルド設定（`BOARD_AVB_KEY_PATH` 等）を確認し、**テスト鍵が使われた場合にビルドを失敗させる
   チェックを CI／リリース工程に組み込む**（前掲の検査ツールはこの用途にも利用可能）。
4. 同一のビルド基盤を共用する他機種についても横展開して確認する。
5. AVB のロールバックインデックス運用（`rollback_index`）の適切な設定を併せて確認する。
6. 工場出荷用ファームウェア（EDL 用の署名済み firehose プログラマを含む）の配布範囲を見直す。
   ただし本質的な問題は署名鍵にあり、配布制限のみでは対策になりません（プログラマは既に流通しているため）。
   **1.（製品固有鍵での署名）が必須の対策**です。

### 4-6. 検証環境

| 項目 | 内容 |
| --- | --- |
| 検証対象 1 | Rakuten Hand 5G（P780）／検証時点の最新ファームウェア（実機） |
| 検証対象 2 | dtab Compact d-52C／ビルド番号 202502141452（検証時点の最新ファームウェア、実機） |
| ブートローダの状態 | 両機種とも **ロック状態のまま**（アンロック操作は実施していない） |
| 書き込み経路 | Rakuten Hand 5G：ローカル権限昇格による root 権限経由／dtab Compact d-52C：EDL モード＋公式ツール由来の firehose プログラマ経由 |
| 使用ツール | NCC Group `test_avb_key`（https://github.com/nccgroup/test_avb_key）、AOSP `avbtool`、OpenSSL、adb、EDL/firehose 書き込みツール |
| 参照した鍵 | AOSP `platform/external/avb` の `test/data/` 配下の公開テスト鍵 |

---

## 5. その他

| 項目 | 内容 |
| --- | --- |
| 製品開発者への直接連絡状況 | 連絡していない（IPA / JPCERT/CC 経由での調整を希望）。届出者情報の開発者への開示は可。 |
| 他機関への届出状況 | なし |
| 一般への公表状況 | 未公表（本届出時点） |
| 公表予定 | なし（IPA・JPCERT/CC の調整結果に従う） |
| 関連する過去の届出 | IPA#11335265、IPA#33436631、IPA#47427200（**同一の脆弱性**。本届出は対象製品の追加） |

### 備考

- 本届出は**一次報告**です。以下については、必要に応じて追加で提出いたしますのでお申し付けください。
  - 各機種の詳細なバージョン情報（ビルド番号、Android バージョン、セキュリティパッチレベル）
  - `test_avb_key` および `avbtool info_image` の実行ログ、一致したテスト鍵の特定
  - 改ざんイメージ起動時の画面等の証跡
  - Rakuten Hand 5G で権限昇格に用いた KGSL ドライバの脆弱性の詳細
    （本届出とは別個の脆弱性と考えられるため、必要であれば別途届け出ます）
- 本脆弱性は、Android 端末を製造・販売する複数の事業者において繰り返し発生している実装上の不備であり、
  当方は同一原因の事象を過去 3 件（IPA#11335265、IPA#33436631、IPA#47427200）届け出ています。
  今回は新たに 2 機種で確認し、**いずれも実機で攻撃の成立まで確認**しました。
  特に dtab Compact d-52C については、**他の脆弱性を一切利用せず、端末への物理アクセスのみで**
  恒久的な改ざんが成立する点で影響が大きいと考えます。
  個別製品への対応に加えて、**業界全体への注意喚起**をご検討いただけますと幸いです。
- 検証は当方が正当に保有する端末に対してのみ実施しており、第三者の端末・システムへのアクセスや、
  実環境への攻撃、攻撃コードの公開は一切行っていません。
